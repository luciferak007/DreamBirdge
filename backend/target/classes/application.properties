spring.application.name=job-portal-backend
server.port=8080

spring.datasource.url=jdbc:postgresql://dpg-d7ms7f3eo5us73evh3ag-a:5432/jobportaldb_zijz
spring.datasource.username=jobportaldb_zijz_user
spring.datasource.password=qNkWMg1AcTulaJQB47xFetC8vmazMLIO

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
app.jwt.secret=ZmFrZUp3dFNlY3JldEtleUZvckpvYlBvcnRhbEFwcGxpY2F0aW9uMTIzNDU2Nzg5MA==
app.jwt.expiration-ms=86400000

# CORS
app.cors.allowed-origins=https://dreambirdge-1.onrender.com

# ============ ADMIN ACCOUNT (single, predefined) ============
# These three values define the ONLY admin account that can ever exist.
# - Change app.admin.email to lock the admin role to a different email.
# - Change app.admin.password BEFORE first boot (or update via DB after).
# Public registration with role=ADMIN or with this email is rejected (see AuthService).
app.admin.email=admin@jobportal.com
app.admin.password=Admin@12345
app.admin.full-name=System Administrator

# ============ FILE UPLOADS (resumes) ============
app.upload.dir=./uploads/resumes
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB
